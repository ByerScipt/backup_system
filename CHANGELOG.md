# Changelog

## v1.1.0

- 解压器在创建输出前绑定 BKP2 的 `packedSize`，并在每个输出块上实施硬上限。
- 还原改为目录文件描述符、`O_NOFOLLOW` 和同目录临时节点，默认以原子不可覆盖方式提交。
- 严格检查归档中央索引尺寸和保留字段，并完整检查 `fsync`、`close` 及父目录同步结果。
- 服务器运行期间及时回收已结束会话，避免线程对象随累计连接数增长。
- 18 种组合升级为完整树比较，新增解压超输出、索引边界和确定性路径竞态回归。
- 保持原有精简目录结构；交付脚本改用安装式二进制，去除构建期 RUNPATH。

## v1.0.0

- 冻结 BKP2 版本 1 归档格式、CLI 契约和网络协议版本 1。
- 通过 18 种算法组合、解析攻击、真实特殊节点、网络隔离/重启/取消、受限内存、
  Qt6 实编译与离屏启动以及 ASan/UBSan 发布门禁。
- 将课程文档与生成物放在同级 `backup_system_delivery`，源码工程保持独立纯净。

## 测试版里程碑

- v0.8：补齐随机二进制、长路径、深目录和纳秒时间戳验收。
- v0.7：本地归档与网络下载采用原子不可覆盖提交。
- v0.6：以 inode/device 与安全文件描述符强化源文件一致性。
- v0.5：远程分块传输提供真实进度与取消清理。
- v0.4：服务端用户目录和文件实施 0700/0600 权限隔离。
- v0.3：严格拒绝 Huffman 尾数据和断链输出覆盖。
- v0.2：拒绝归档内符号链接下面的子条目。
- v0.1：完成核心、CLI、Qt6 GUI、网络服务器和初始自动化测试。
